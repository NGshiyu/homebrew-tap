interface:
  display_name: "Homebrew Tap"
  short_description: "Repo-specific patterns and workflows for homebrew-tap"
  default_prompt: "Use the homebrew-tap repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true